const question = document.getElementById('flashcard-question');
const answer = document.getElementById('flashcard-answer');
const title = document.getElementById('flashcard-title');
const prev = document.getElementById('prev');
const next = document.getElementById('next');
const error = document.getElementById('error');
const delElement = document.getElementById('delete');
const saveButton = document.getElementById('save');
const questionNumber = document.getElementById('question-number');

export { question, answer, title, prev, next, error, delElement, saveButton, questionNumber }